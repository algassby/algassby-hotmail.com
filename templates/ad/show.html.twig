{% extends 'base.html.twig' %}

{% block title %}
    {{ ad.title }}
{% endblock %}

{% block body %}
<div class="ad-title" style="background-image: url({{ ad.coverImage }})" >
    <div class="container">
        <h1>{{ ad.title }}</h1>
        <p>{{ ad.introduction }}</p>
        <p class="h2"><strong>{{ ad.rooms }} chambres</strong> pour <strong>{{ ad.price }} &euro</strong> par nuit</p>
        <a class="btn btn-primary" href="#">reserver</a>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-8 mb-3">
            {{ ad.content|raw }}
            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                    {% for image in ad.yes %}{#yes est la collection d'images#}
                        <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" {% if loop.first %}class="active"{% endif %}></li>
                    {% endfor %}
                </ol>
                <div class="carousel-inner">
                    {% for image in ad.yes %}
                        <div class="carousel-item {% if loop.first %}active{% endif %}">{#yes est la collection d'images#}
                        <img src="{{image.url}}" class="d-block w-100" alt="">
                        </div>
                        <div class="carousel-caption d-none d-md-block">
                            <h5>{{ ad.title }}</h5>
                            <p>{{ image.caption  }}</p>
                        </div>
                    {% endfor %}
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
                </div>
        </div>
        <div class="col">
            <div class="row">
                <div class="col-4">
                    <img src="http://placehold.it/64x64" alt="Avatar de Barry">
                </div>
                <div class="col">
                    <h4>Algassimou Barry</h4>
                    <span class="badge badge-primary">3 annonces</span>
                </div>
            </div>
            <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Vel, repudiandae necessitatibus dolorum cupiditate excepturi eos temporibus harum suscipit et omnis!</p>
            <p>Officia veritatis dolores vel adipisci non! Animi natus, aut eaque, odit voluptates esse earum qui fugiat itaque quo illum eveniet.</p>
            <p>Quisquam, laboriosam, cumque molestiae odio in dolorum alias harum voluptates aspernatur deserunt iure nam eos sit labore accusantium, repellendus tempore.</p>
        </div>
    </div>
</div>

{% endblock %}
